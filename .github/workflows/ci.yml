name: ci

on: pull_request
jobs:
    test: 
        runs-on: ubuntu-latest

        steps:
            - name: primeira CI realizada
              run: echo hello world

            - name: verificar o que tem na pasta
              run: echo $(ls)

            - name: verificar o diretório
              run: echo $(pwd)
